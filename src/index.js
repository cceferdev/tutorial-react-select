import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Filter from './forEachConMap';


ReactDOM.render(
  <React.StrictMode>
    <forEachConMap />
  </React.StrictMode>,
  document.getElementById('root')
);
